# Icon assets

The packaged `icon.png` (64×64) currently lives next to the csproj at
`src/NextIteration.SpectreConsole.SelfUpdate/icon.png`. It is a placeholder
copied from `NextIteration.SpectreConsole.Auth` so the package builds.

Before the first public release, replace it with a SelfUpdate-specific mark
that fits the visual family established by the sibling packages (Auth's
shield-in-circle, Splash's typographic mark). Drop the SVG source here as
`NextIteration.SpectreConsole.SelfUpdate.svg` for future regeneration.
